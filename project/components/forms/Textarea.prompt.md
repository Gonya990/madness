Multi-line text input, with the same label/hint/error chrome as `Input`.

```jsx
<Textarea label="Description" rows={5} placeholder="What does this project do?" />
<Textarea label="Run command" mono defaultValue="npm run dev" />
```

- Vertically resizable. Use `mono` for code/config snippets.
