An on/off toggle. Use for settings that apply immediately; use Checkbox for form selections that submit.

```jsx
<Switch label="Auto-save" defaultChecked />
<Switch label="Vim mode" size="sm" />
```

- Controlled via `checked` + `onChange`, or uncontrolled via `defaultChecked`.
